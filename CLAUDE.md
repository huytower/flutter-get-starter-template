# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

@AGENTS.md

The file above is the canonical, tool-agnostic rulebook for this repo (architecture rules, commands, DI
convention, design-system rules, naming, verification checklist, CodeGraph usage). It applies in full to Claude
Code — there is nothing Claude-specific to add on top of it today. Do not re-add duplicated rule content here;
if something Claude-specific comes up, add it below this line, keep `AGENTS.md` as the source of truth for
everything else, and don't restate what's already there.
