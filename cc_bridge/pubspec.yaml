name: cc_bridge
description: A bridge module for cross-feature communication and navigation contracts.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '^3.12.0'
  flutter: '^3.44.3'

resolution: workspace

dependencies:
  cc_sdk_data:
    path: ../shared/cc_core_sdk/cc_sdk_data
  cc_sdk_ui:
    path: ../shared/cc_core_sdk/cc_sdk_ui
  flutter:
    sdk: flutter
  injectable: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  injectable_generator: ^3.1.3
  build_runner: ^2.15.1
