name: cc_bridge
description: A bridge module for cross-feature communication and navigation contracts.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '^3.12.0'

resolution: workspace

dependencies:
  cc_sdk_data:
    path: cc_core_sdk/cc_sdk_data
  flutter:
    sdk: flutter
  injectable: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  injectable_generator: ^3.0.2
  build_runner: ^2.15.0
