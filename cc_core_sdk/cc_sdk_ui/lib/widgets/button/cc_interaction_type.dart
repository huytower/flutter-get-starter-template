/// Defines the type of interaction to apply to a button.
enum CcInteractionType {
  /// No interaction - static button only.
  none,

  /// Simple tap with no animation.
  tap,

  /// Bounce animation on tap.
  bounce,
}
